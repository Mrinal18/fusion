name: "pretraining"
shuffle: True
drop_last: True
is_only_one_pair_per_subject: False
use_balanced_sampler: False
only_labeled: False
args:
  logdir: './'
  num_epochs: 50
  verbose: true
  resume: false
  timeit: true