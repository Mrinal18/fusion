name: "AE"
args:
  sources: ${experiment.sources}
  architecture: ${experiment.architecture.name}
  architecture_params: ${experiment.architecture.args}
  pretrained_checkpoint: ${experiment.pretrained_checkpoint}